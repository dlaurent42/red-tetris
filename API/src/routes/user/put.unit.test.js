import request from 'supertest';
import express from 'express';
import Router from '../../helpers/Router';

import { TOKENS } from '../../config/constants';

// const app = express();
// new Router(app).setAllRoutes();


describe('/user/:id testing', () => {

	it('testing with invalid id', () => {
		// return request(app)
		// 	.put('/user/asd')
		// 	.set('Authorization', `Bearer ${TOKENS.AUTH}`)
		// 	.send({})
		// 	.expect(200)
		// 	.end();
		// return request(app)
		// 	.get('/leaderboard')
		// 	.set('Authorization', `Bearer ${TOKENS.AUTH}`)
		// 	.expect(200)
		// 	.end();

		// expect(res).toHaveLength(128);
	});

});
